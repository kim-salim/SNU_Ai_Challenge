# Weights Directory

Place local pretrained SigLIP2 files under:

`weights/pretrained/siglip2_base_224/`

Trained heads are written under:

`weights/heads/`

These directories are intentionally ignored by git.

