# Data Directory

Place competition files under `data/raw/`:

- `train.csv`
- `test.csv`
- `sample_submission.csv`
- frame image files or directories referenced by those CSV files

`data/raw/` and `data/features/` are intentionally ignored by git.

